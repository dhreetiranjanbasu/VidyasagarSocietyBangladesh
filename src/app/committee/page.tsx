import SectionTitle from "@/components/SectionTitle"; import { readData } from "@/lib/data"; import type { CommitteeMember } from "@/lib/types";
export const metadata={title:"Executive Committee"}; export default async function Page(){const c=await readData<CommitteeMember[]>("committee.json"); return <main><section className="bg-[#153f33] py-20 text-white"><div className="container-page"><SectionTitle inverse eyebrow="Leadership / নির্বাহী কমিটি" title="Executive Committee" text="Demo profiles based on the supplied letterhead and public reporting. Verify the complete current committee before launch."/></div></section><section className="container-page grid gap-6 py-20 sm:grid-cols-2 lg:grid-cols-3">{c.map((m,i)=><article key={m.name} className="rounded-[1.7rem] border border-slate-200 bg-white p-7"><div className="grid size-16 place-items-center rounded-full bg-[#f8f3e9] font-serif text-xl font-black text-[#087a4b]">{m.name.split(' ').slice(-1)[0].slice(0,2).toUpperCase()}</div><div className="mt-5 text-xs font-bold uppercase tracking-wider text-[#b99245]">{m.role}</div><h2 className="mt-2 text-xl font-bold text-[#153f33]">{m.name}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{m.bio}</p></article>)}</section></main>}
