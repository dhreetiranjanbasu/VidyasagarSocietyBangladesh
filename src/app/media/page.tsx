import SectionTitle from "@/components/SectionTitle"; import { readData } from "@/lib/data";
type Media={title:string;category:string;year:string}; export const metadata={title:"Media"}; export default async function Page(){const m=await readData<Media[]>("media.json"); return <main><section className="bg-[#f8f3e9] py-20"><div className="container-page"><SectionTitle eyebrow="Media / গ্যালারি" title="Stories, moments and archives" text="Replace these demo tiles with verified Society photos and videos."/></div></section><section className="container-page grid gap-5 py-20 sm:grid-cols-2 lg:grid-cols-3">{m.map((x,i)=><article key={x.title} className="overflow-hidden rounded-[1.7rem] border border-slate-200 bg-white"><div className="paper-grid grid aspect-[4/3] place-items-center bg-[#f8f3e9] text-6xl font-serif text-[#087a4b]/25">0{i+1}</div><div className="p-6"><div className="text-xs font-bold uppercase text-[#b99245]">{x.category} · {x.year}</div><h2 className="mt-2 text-xl font-bold text-[#153f33]">{x.title}</h2></div></article>)}</section></main>}
