import SectionTitle from "@/components/SectionTitle"; import ContactForm from "@/components/ContactForm"; import { readData } from "@/lib/data"; import type { SiteData } from "@/lib/types"; import { Mail, MapPin, Phone } from "lucide-react";
export const metadata={title:"Contact"}; export default async function Page(){const s=await readData<SiteData>("site.json"); return <main><section className="bg-[#f8f3e9] py-20"><div className="container-page"><SectionTitle eyebrow="Contact / যোগাযোগ" title="Connect with Vidyasagar Society Bangladesh"/></div></section><section className="container-page grid gap-10 py-20 lg:grid-cols-[.8fr_1.2fr]"><div className="grid content-start gap-5"><Info icon={<Phone/>} t="Phone" x={s.phone}/><Info icon={<Mail/>} t="Email" x={s.email}/><Info icon={<MapPin/>} t="Office" x={s.address}/><p className="mt-3 text-sm leading-7 text-slate-500">Please verify the office address and contact details with the Society before production deployment.</p></div><ContactForm/></section></main>}
function Info({icon,t,x}:{icon:React.ReactNode;t:string;x:string}){return <div className="flex gap-4 rounded-2xl border border-slate-200 bg-white p-5"><div className="text-[#087a4b]">{icon}</div><div><div className="text-sm font-bold text-[#153f33]">{t}</div><div className="mt-1 text-sm leading-6 text-slate-600">{x}</div></div></div>}
