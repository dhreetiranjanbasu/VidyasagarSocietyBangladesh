import SectionTitle from "@/components/SectionTitle"; import { readData } from "@/lib/data"; import type { Publication } from "@/lib/types"; import { BookOpen, Download } from "lucide-react";
export const metadata={title:"Publications"}; export default async function Page(){const p=await readData<Publication[]>("publications.json"); return <main><section className="bg-[#f8f3e9] py-20"><div className="container-page"><SectionTitle eyebrow="Publications / প্রকাশনা" title="Books, commemorative works and research" text="A demo archive structure ready for verified titles, cover images and PDF files."/></div></section><section className="container-page grid gap-6 py-20 md:grid-cols-2 lg:grid-cols-3">{p.map(x=><article key={x.id} className="heritage-card rounded-[1.7rem] bg-white p-7"><div className="grid aspect-[4/2.8] place-items-center rounded-2xl bg-[#153f33] text-white"><BookOpen size={48}/></div><div className="mt-5 text-xs font-bold uppercase tracking-wider text-[#b99245]">{x.type} · {x.year}</div><h2 className="mt-2 text-xl font-bold text-[#153f33]">{x.title}</h2><div className="mt-1 text-sm text-slate-400">{x.author}</div><p className="mt-4 text-sm leading-7 text-slate-600">{x.description}</p><button className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#087a4b]"><Download size={16}/> PDF placeholder</button></article>)}</section></main>}
