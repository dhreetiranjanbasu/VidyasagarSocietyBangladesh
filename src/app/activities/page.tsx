import SectionTitle from "@/components/SectionTitle"; import { readData } from "@/lib/data"; import type { Activity } from "@/lib/types";
export const metadata={title:"Activities"}; export default async function Page(){const a=await readData<Activity[]>("activities.json"); return <main><section className="bg-[#153f33] py-20 text-white"><div className="container-page"><SectionTitle inverse eyebrow="Activities / কার্যক্রম" title="Ideas translated into programmes" text="Seminars, publications, cultural dialogue, youth engagement and educational initiatives."/></div></section><section className="container-page grid gap-5 py-20 md:grid-cols-2 lg:grid-cols-3">{a.map((x,i)=><div key={x.title} className="rounded-[1.7rem] border border-emerald-950/10 bg-white p-8"><span className="text-xs font-black text-[#b99245]">0{i+1}</span><h2 className="mt-5 text-2xl font-bold text-[#153f33]">{x.title}</h2><p className="mt-3 leading-8 text-slate-600">{x.text}</p></div>)}</section></main>}
