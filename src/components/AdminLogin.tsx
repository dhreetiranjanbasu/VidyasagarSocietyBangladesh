"use client"; import { FormEvent,useState } from "react"; import { useRouter } from "next/navigation";
export default function AdminLogin(){const [err,setErr]=useState(""); const r=useRouter(); async function go(e:FormEvent<HTMLFormElement>){e.preventDefault(); const p=new FormData(e.currentTarget).get("password"); const x=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:p})}); if(x.ok)r.push("/admin"); else setErr("Invalid demo password")}; return <form onSubmit={go} className="mx-auto mt-10 grid max-w-md gap-4 rounded-[2rem] bg-white p-8 shadow-xl"><input name="password" type="password" placeholder="Admin demo password" className="rounded-xl border border-slate-200 px-4 py-3"/><button className="rounded-full bg-[#087a4b] px-6 py-3 font-bold text-white">Login</button>{err&&<p className="text-sm font-bold text-red-600">{err}</p>}<p className="text-xs leading-6 text-slate-400">Default: VSB@Demo2026 (change through .env.local)</p></form>}
