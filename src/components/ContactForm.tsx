"use client";
import { FormEvent, useState } from "react";
export default function ContactForm(){const [msg,setMsg]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))}); setMsg(r.ok?"Message saved locally for the demo.":"Unable to save message."); if(r.ok)e.currentTarget.reset()} return <form onSubmit={submit} className="grid gap-4 rounded-[2rem] bg-white p-7 shadow-xl shadow-emerald-950/5"><input required name="name" placeholder="Your name" className="rounded-xl border border-slate-200 px-4 py-3"/><input required name="email" type="email" placeholder="Email" className="rounded-xl border border-slate-200 px-4 py-3"/><input name="subject" placeholder="Subject" className="rounded-xl border border-slate-200 px-4 py-3"/><textarea required name="message" rows={6} placeholder="Message" className="rounded-xl border border-slate-200 px-4 py-3"/><button className="rounded-full bg-[#087a4b] px-6 py-3 font-bold text-white">Send Demo Message</button>{msg&&<p className="text-sm font-semibold text-[#087a4b]">{msg}</p>}</form>}
