"use client";
import { FormEvent, useState } from "react";
export default function MembershipForm(){const [msg,setMsg]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); setBusy(true); const form=new FormData(e.currentTarget); const body=Object.fromEntries(form.entries()); const r=await fetch("/api/membership",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}); setMsg(r.ok?"Application saved in the local demo data file.":"Could not save application."); if(r.ok)e.currentTarget.reset(); setBusy(false)} return <form onSubmit={submit} className="grid gap-5 rounded-[2rem] bg-white p-6 shadow-xl shadow-emerald-950/5 md:p-8"><div className="grid gap-5 md:grid-cols-2"><Field name="name" label="Full name"/><Field name="email" label="Email" type="email"/><Field name="phone" label="Phone"/><label className="grid gap-2 text-sm font-semibold">Membership type<select name="type" className="rounded-xl border border-slate-200 px-4 py-3"><option>General Member</option><option>Life Member</option><option>Student Member</option><option>Associate Member</option></select></label></div><label className="grid gap-2 text-sm font-semibold">Why would you like to join?<textarea name="message" rows={5} className="rounded-xl border border-slate-200 px-4 py-3"/></label><button disabled={busy} className="rounded-full bg-[#087a4b] px-6 py-3 font-bold text-white disabled:opacity-50">{busy?"Saving...":"Submit Demo Application"}</button>{msg&&<p className="text-sm font-semibold text-[#087a4b]">{msg}</p>}</form>}
function Field({name,label,type="text"}:{name:string;label:string;type?:string}){return <label className="grid gap-2 text-sm font-semibold">{label}<input required name={name} type={type} className="rounded-xl border border-slate-200 px-4 py-3"/></label>}
